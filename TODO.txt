# installare lightning
# capire come funziona il checkpoint per i pesi quando l'accuratezza migliora durante il train
# scrivere la cnn
# capire quali parametri possiamo modificare (lr, batch_size, epochs, numero di strati)
# trovare il dataset
# impostare il dataloader per pytorch
# capire se serve preprocessing delle immagini (probabilmente no)

# salvare le accuracy per stampare con matplotlib il grafico
# capire quali immagini vengono usate ogni epoca (se sono le stesse ad esempio per ogni primo batch), e che trasformazioni hanno
fare interfaccia grafica (probabilmente no)
# aggiungere blocchi convoluzionali
# provare U-Net (?) NO, QUELLA FUNZIONA PER LA SEGMENTAZIONE
# provare su mnist per vedere se funziona, OK FUNZIONA
# matrice di confusione

# CARICA TUTTO SU COLAB E PROVA CON MNIST PER 50 EPOCHE
COSA NON SONO RIUSCITO A FARE PER ORA

implementare scheduler per il learning rate
    tracciare la val acc con e senza scheduler